<Window x:Class="DatenMeister.AddOns.ComplianceSuite.WPF.SelectSuite"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="300" SizeToContent="Height"
             WindowStartupLocation="CenterScreen"
            Title="Select Extent"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">
             
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>            
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <RadioButton x:Name="radioGeneric" Grid.Column="0" Grid.Row="0" Margin="10">Test Generic Extents</RadioButton>
        <RadioButton x:Name="radioXml" Grid.Column="0" Grid.Row="1" Margin="10">Test Xml Extent</RadioButton>
        <Button Grid.Row="2" IsDefault="True" Click="Run_Click" Margin="10" Padding="10">Run Compliance Suite</Button>
    </Grid>
</Window>
